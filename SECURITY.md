# 安全说明

本仓库**不含使用者的真实凭据** —— 没有真实 WiFi 账号密码，没有 API 密钥。

代码里保留了若干**示例**地址（配网门户用的 `192.168.4.1`、历史 daemon 的示例 `192.168.1.100:8899`）——
示例本身不属于隐私泄露。**但不要把示例改成你自己的真实内网地址再提交**（见第四节）。

所有配置都在设备端由用户自己填写（见 README「第二步」）。

---

## 一、当前主工程（`arduino/integrated/`）

### ⚠️ 部署前提：仅用于**可信局域网**

请把设备当作一台**局域网内可见、且没有访问控制**的小服务器来对待：

- **不要**把它映射到公网（不要做端口转发 / 反向代理 / 内网穿透）。
- 不要放在不可信的公共 WiFi 下。
- 同一局域网内的任何人都能打开控制台并修改设备设置。

### 已知限制（如实列出，尚未修复）

| # | 限制 | 说明 |
|---|---|---|
| 1 | **WiFi 密码经 GET 提交** | 「更换 WiFi」页的表单用 `method='GET'` 提交，密码会出现在请求 URL 里，可能被浏览器历史、代理日志记录。**后续应改为 POST**（注意：POST 本身不提供加密） |
| 2 | **控制台无认证** | 全部路由（`/set`、`/stock_save`、`/change_wifi`、`/reset_wifi`、`/do_upload` 等）都没有口令校验，任何能访问该 IP 的人都能操作设备 |
| 3 | **控制台为明文 HTTP** | 局域网内可被嗅探。修改 WiFi 密码时尤其注意 |
| 4 | **HTTPS 行情跳过证书校验** | 分时/日K 请求使用 `setInsecure()`，不校验服务器证书。这是为省 Flash/RAM 做的取舍，**不防中间人**，请知悉 |

### 数据存放位置

- WiFi 凭据、自选股、城市、亮度等：**设备自身 NVS**（`Preferences`）
- 图片与城市数据：**设备自身 SPIFFS**
- 设备**不会**把使用者的配置或图片上传到项目作者的服务器（详见 README「数据接口说明」）

> README 的「数据接口说明」里列出了设备与浏览器各自会访问的第三方公开服务，
> 建议一并阅读 —— 那里说明了数据会流向哪些外部服务。

---

## 二、历史模块（`daemon/`）

`daemon/app.py` 是项目**早期**（v0.1）的 PC 端 Flask 中转服务，仅配合 `arduino/stocktv` 使用，
**当前主工程不需要它**。

- 它默认在局域网内监听 **8899 端口，且没有任何认证**。
- **不要**把 8899 直接暴露到公网。若确需在可信局域网外使用，请自行加防火墙规则或认证层。

> 早期版本的说明文字要求用户"修改源码里的 `YOUR_WIFI_SSID` / `YOUR_WIFI_PASSWORD` 常量"。
> **现在两个工程都不需要了**：主工程的配网全部在设备的 AP 配网页面上完成（见 README「第二步」）；
> `firmware/` 调试工程是纯屏显自检，**已删掉那段联网代码** —— 它 `config.h` 里的
> `WIFI_SSID` / `WIFI_PASS` 是**未被使用的占位符**。
>
> ⚠️ 那两个占位符**绝不能被传进 `WiFi.begin()`**：即使连不上，ESP32 的 WiFi 驱动也会把这对
> 字符串写进 NVS，**顶掉使用者真实的配网凭据**。（该工程源码里已有同款警告。）

---

## 三、设备里存有你的 WiFi 密码

设备要连你的 WiFi，就必须保存账号密码。请知悉：

- **存放位置**：设备自身 NVS，**默认未做加密保护**。
  能物理接触设备并读取其 flash 的人，可以从中取出 WiFi 密码。
  （WiFi 凭据由 WiFi 驱动保存；固件自己的 `Preferences` 里存的是自选股、城市、亮度等设置。）
- **通信方式**：配网与「更换 WiFi」都在局域网明文 HTTP 页面上完成（见第一节第 3 条）；
  「更换 WiFi」的表单还是 **GET 提交**，密码会进入 URL（见第一节第 1 条）。

### ⚠️ 配网期间的热点**没有密码**

设备未配网（或连不上已保存的 WiFi）时会开一个热点 **`SDD小电视`**，
而它是**开放热点** —— 代码是 `wm.autoConnect("SDD小电视")`，只传了名称，**没有设密码**。
你填的 WiFi 密码恰好就提交在这个门户里。

- **配网期间，附近任何人都能连上这个热点**，也就能打开配网门户。
- 代码设了 `setConfigPortalTimeout(180)`：门户 **3 分钟**无操作自动关闭 ——
  这缩短了窗口，但**不会**让热点在开放期间变安全。
- ⚠️ **三分钟不是"累计开放时间上限"**：门户超时后固件会**立即重启自己**（`ESP.restart()`），
  重启后若仍连不上，门户就**再开三分钟** —— 只要一直连不上，
  "开门户 → 三分钟 → 重启"就会一直循环，看上去就像热点一直在。**它不会自己放弃。**
- 设备**同时只有一个热点**（代码里没有任何常开的 `softAP`）：一旦连上你保存的 WiFi 就关闭它。
  但若某次开机连不上，它会重新打开门户。

⇒ **做法：在你自己信任的环境里上电，并尽快把 WiFi 配好。**
不要让设备长时间停留在配网状态，尤其**不要在陌生场所 / 公共环境**这么做。

### ⇒ 作礼物送出、作商品转卖、更换设备之前

| 建议做法 | 实际效果 |
|---|---|
| 网页控制台「重置 WiFi」 | 清除 NVS 里的 WiFi 凭据 + 本固件的偏好设置（`prefs.clear()` 与 `WiFi.disconnect(…, true)`），设备回到 AP 配网模式。**不会清除 SPIFFS** |
| 重新烧录一版不配网的固件 | ⚠️ **清不掉凭据。** 普通烧录（不勾选 "Erase All Flash"）**不会去擦 NVS 分区**，所以 NVS 原样保留（实测：跑过不配网固件前后，NVS 分区 sha256 逐字节相同）。<br>⚠️ **别把这条读成"烧录只会写 app 区"** —— 常规烧录（含 Arduino IDE 的"上传"）还会一起写 bootloader 与分区表，只是这几个区里**都没有 NVS**。 |
| 最彻底：整片 flash 擦除 | `esptool.py erase_flash` —— 把整片 flash 抹掉，分区表、bootloader、NVS、SPIFFS 一起重来。**最省事的清法。**<br>严格讲它不是"技术上唯一"：`erase_region 0x9000 0x5000` 与 `erase_region 0x210000 0x1F0000` 也能分别擦掉 NVS 与 SPIFFS（地址见 `firmware/no_ota.csv`），只是要自己填。 |

> ⚠️ 上面这几个 esptool 子命令是 **4.x 的写法**：**5.x 把下划线换成连字符**
> （`erase_flash`→`erase-flash`、`erase_region`→`erase-region`、`read_flash`→`read-flash`）。
> 混用会直接报 `invalid choice`。详见 [`firmware/README.md`](firmware/README.md)。

> ⚠️ **转让/换机时别只按「重置 WiFi」**：它只清 NVS，
> **SPIFFS 里你上传的相册图片、网页背景、城市数据仍然都在**。
> 而控制台**没有"删除某个文件"的功能**（固件里的 `SPIFFS.remove` 只出现在内部清理里：
> 开机按固定名单删旧版本遗留文件、清上传临时文件 `/upload.tmp`、替换完成后清 `.bak` 备份
> —— 没有一个是"删掉你上传的那张图"的入口），
> 想彻底清干净就用**整片擦除**（或按上表的 `erase_region` 分别擦两个分区）；
> 否则只能用一张新图**覆盖**掉旧的那张。

> ⚠️ 也因为这一点：**尽量不要把设备借给不信任的人，也不要在不可信的网络上配网。**

---

## 四、附：给本仓库的贡献者 / 二次开发者

> 👆 **本节受众不是使用者，而是向本仓库提交代码、或基于本仓库二次开发的人。**

提交前请自查：本仓库**不含真实凭据**（`.gitignore` 已排除 `.env`、`secrets.*`、
`credentials.*`、`*.pem`、`*.key`），别把本地填入的 WiFi 账号密码或内网 IP 一起提交 ——
**一旦推送过就不可撤销**（Git 历史与可能的 fork 里都还在），届时只能轮换凭据。
