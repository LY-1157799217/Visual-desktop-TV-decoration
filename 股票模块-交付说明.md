# stock-tv 股票模块（ESP32-C3）交付说明

> 本文档是股票功能模块的完整快照，用于后续"原厂功能 + 股票模块"结合时快速恢复/参考。

## 一、硬件

| 项 | 参数 |
|----|------|
| 芯片 | ESP32-C3（QFN32，160MHz，320KB RAM，4MB Flash，WiFi+BLE） |
| 屏幕 | ST7789P3 1.54" 240x240 TFT（金逸晨 GMT154-07，12P接口） |
| 背光 | P-MOS，低电平点亮 |

## 二、引脚映射（已从电路图坐标证实）

```
SCL = IO3    SDA = IO5    DC = IO2    RST = IO6    BL = IO1    CS = GND(接地)
```

> 注意：原始 stock-tv 配置 SDA=IO4/RST=IO5 是**错的**（把模块引脚编号当成了 GPIO 编号）。数据线接错会"背光亮但纯黑"。

## 三、软件参数（当前顺畅形态）

- **库**：TFT_eSPI 2.5.43（硬件SPI）+ ArduinoJson 7.x
- **SPI**：10MHz，`setRotation(0)`，`invertDisplay(true)`
- **轮播**：ROTATE_MS = 5000（5秒/只）
- **分片刷新**：REFRESH_MS/SYMBOL_COUNT = 15秒/只（60秒一轮），**只更新数据不重绘**（重绘会 fillScreen 导致原地闪屏）
- **HTTP**：超时 2000ms
- **WiFi**：`setTxPower(8_5dBm)`（降功率，省电）
- **走势图**：SPARK_POINTS = 48 点 sparkline
- **配色**：红涨绿跌（UP=0xF800红 / DOWN=0x07E0绿 / FLAT=0xBDF7）
- **布局**：名称(textSize2顶部) + 价格(textSize4大字) + 涨跌幅(textSize2) + 走势图(224x80) + 代码(textSize1底部)

## 四、自选列表

```
sh000001  上证指数
sh600519  贵州茅台
sz300308  中际旭创
f005827   易方达蓝筹（场外基金，无分时走势图）
```

## 五、烧录步骤（Arduino IDE）

1. **安装库**：工具→管理库，装 `TFT_eSPI` 和 `ArduinoJson`（7.x）
2. **配置 TFT_eSPI 引脚**：在本机 Arduino 库目录的 `TFT_eSPI/User_Setup.h` 中使用上表引脚
3. **打开固件**：打开仓库中的 `arduino/stocktv/stocktv.ino`
4. **开发板**：ESP32C3 Dev Module；**端口**：选择设备管理器显示的实际端口；**Upload Speed**：115200
5. **上传**（点击 → 按钮）
6. **串口监视器**：波特率 **115200**（与固件 `Serial.begin(115200)` 一致）

## 六、文件位置

| 文件 | 位置 |
|------|------|
| 固件源码 | 仓库中的 `arduino/stocktv/stocktv.ino` |
| TFT_eSPI 引脚配置 | 本机 Arduino 库目录中的 `TFT_eSPI/User_Setup.h` |
| 数据源 daemon | 仓库中的 `daemon/app.py`（Flask 8899端口） |
| 硬件资料/电路图 | 请根据自己的硬件资料目录配置 |

## 七、数据流

```
腾讯行情(股票/指数) ─┐
                     ├→ PC daemon(app.py, 端口8899) ─→ WiFi ─→ ESP32-C3 固件 ─→ 屏幕轮播
东财基金(场外净值) ─┘
```

固件通过配置的 daemon 地址（例如 `http://<your-lan-host>:8899/quote?symbol=xxx&points=48`）取 JSON：
```json
{"symbol":"sh600519","name":"贵州茅台","price":1341.99,"change":-13.3,"changePct":-0.98,"ok":true,"spark":[48个点]}
```

## 八、已知取舍

- **中文名需字库**（现用英文标签 Moutai/Innolight 等，TFT_eSPI 默认字体不支持中文）
- **场外基金无分时**（f005827 的 spark 为空，无走势图）
- **分片刷新不重绘** → 当前股价格更新延迟 ≤20秒
